package com.vcet.campushire.repository;

import com.vcet.campushire.entity.PlacementEvent;
import org.springframework.data.jpa.repository.JpaRepository;

public interface PlacementEventRepository extends JpaRepository<PlacementEvent, Long> {
}
