spring:
  application:
    name: campushire

  datasource:
    url: jdbc:postgresql://localhost:5432/campushire
    username: postgres
    password: Ragul
    driver-class-name: org.postgresql.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true
        dialect: org.hibernate.dialect.PostgreSQLDialect

server:
  port: 8091

logging:
  level:
    org:
      springframework:
        security: DEBUG